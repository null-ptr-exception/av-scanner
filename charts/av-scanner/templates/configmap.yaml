apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "av-scanner.fullname" . }}-inventory
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "av-scanner.labels" . | nindent 4 }}
data:
  hosts: |
    {{- .Values.inventory | nindent 4 }}
